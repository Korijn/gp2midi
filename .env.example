# Copy to .env (which is not committed) to run the checks against real scores.
# The folder holding your .gp files; Guitar Pro's own exports may sit in a "GP Exports" subfolder.
GP2MIDI_TEST_SONGS=/path/to/tabs
